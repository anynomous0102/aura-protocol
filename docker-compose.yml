services:
  aura-api-gateway:
    build:
      context: .
      dockerfile: docker/Dockerfile.api-gateway
    container_name: aura-api-gateway
    env_file:
      - ./backend/.env
    environment:
      AURA_ALLOWED_ORIGINS: ${AURA_ALLOWED_ORIGINS:-http://localhost:3000,http://127.0.0.1:3000}
      AURA_DATA_DIR: /app/backend/data
      AURA_REGION: ${AURA_REGION:-local}
      AURA_REGION_ID_PREFIX: ${AURA_REGION_ID_PREFIX:-local}
      AURA_ENVIRONMENT: ${AURA_ENVIRONMENT:-development}
      AURA_SENTINEL_DIAGNOSTICS_ENABLED: "true"
      AURA_SENTINEL_MUTATION_ENABLED: "false"
      AURA_SENTINEL_ADMIN_HMAC_SECRET: ${AURA_SENTINEL_ADMIN_HMAC_SECRET:-dev-only-change-me}
      AURA_SENTINEL_ADMIN_ROLE: ${AURA_SENTINEL_ADMIN_ROLE:-sentinel-admin}
      AURA_SENTINEL_PATCH_KEY: ${AURA_SENTINEL_PATCH_KEY:-}
      DATABASE_ENGINE: cockroach
      COCKROACH_DATABASE_URL: ${COCKROACH_DATABASE_URL:-postgresql+asyncpg://root@aura-cockroach-us-east:26257/defaultdb?ssl=disable}
      DB_TRANSACTION_RETRIES: ${DB_TRANSACTION_RETRIES:-5}
      GLOBAL_REDIS_URLS: ${GLOBAL_REDIS_URLS:-local|redis://aura-redis:6379/0}
      REDIS_GLOBAL_SYNC_MODE: ${REDIS_GLOBAL_SYNC_MODE:-crdt-active-active}
      REDIS_RATE_LIMIT_SCOPE: ${REDIS_RATE_LIMIT_SCOPE:-global}
      REDIS_CONNECT_ATTEMPTS: ${REDIS_CONNECT_ATTEMPTS:-12}
      REDIS_OPERATION_RETRIES: ${REDIS_OPERATION_RETRIES:-4}
      REDIS_ENDPOINT_COOLDOWN_MAX_SECONDS: ${REDIS_ENDPOINT_COOLDOWN_MAX_SECONDS:-30}
      REDIS_RECONNECT_BACKOFF_SECONDS: ${REDIS_RECONNECT_BACKOFF_SECONDS:-0.25}
      REDIS_URL: redis://aura-redis:6379/0
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}
      OTEL_EXPORTER_OTLP_INSECURE: ${OTEL_EXPORTER_OTLP_INSECURE:-true}
    ports:
      - "8000:8000"
    read_only: true
    tmpfs:
      - /tmp:noexec,nosuid,size=128m
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    depends_on:
      - aura-cockroach-init
      - aura-redis
    networks:
      - aura-edge
      - aura-broker
      - aura-data
    restart: unless-stopped

  aura-p2p-node:
    build:
      context: .
      dockerfile: docker/Dockerfile.p2p-node
    container_name: aura-p2p-node
    environment:
      AURA_REGION: ${AURA_REGION:-local}
      AURA_REGION_ID_PREFIX: ${AURA_REGION_ID_PREFIX:-local}
      AURA_ENVIRONMENT: ${AURA_ENVIRONMENT:-development}
      REDIS_URL: redis://aura-redis:6379/0
      GLOBAL_REDIS_URLS: ${GLOBAL_REDIS_URLS:-local|redis://aura-redis:6379/0}
      REDIS_GLOBAL_SYNC_MODE: ${REDIS_GLOBAL_SYNC_MODE:-crdt-active-active}
      REDIS_CONNECT_ATTEMPTS: ${REDIS_CONNECT_ATTEMPTS:-12}
      REDIS_OPERATION_RETRIES: ${REDIS_OPERATION_RETRIES:-4}
      AURA_P2P_PORT: "9000"
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}
      OTEL_EXPORTER_OTLP_INSECURE: ${OTEL_EXPORTER_OTLP_INSECURE:-true}
    read_only: true
    tmpfs:
      - /tmp:noexec,nosuid,size=128m
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    depends_on:
      - aura-redis
    networks:
      - aura-broker
      - aura-p2p
    restart: unless-stopped

  aura-sentinel-core:
    build:
      context: .
      dockerfile: docker/Dockerfile.sentinel-core
    container_name: aura-sentinel-core
    env_file:
      - ./backend/.env
    environment:
      AURA_REPO_ROOT: /workspace
      AURA_REGION: ${AURA_REGION:-local}
      AURA_REGION_ID_PREFIX: ${AURA_REGION_ID_PREFIX:-local}
      AURA_ENVIRONMENT: ${AURA_ENVIRONMENT:-development}
      AURA_SENTINEL_ALLOWED_ROOTS: backend/app
      AURA_SENTINEL_MUTATION_ENABLED: "true"
      AURA_SENTINEL_ADMIN_HMAC_SECRET: ${AURA_SENTINEL_ADMIN_HMAC_SECRET:-dev-only-change-me}
      AURA_SENTINEL_ADMIN_ROLE: ${AURA_SENTINEL_ADMIN_ROLE:-sentinel-admin}
      AURA_SENTINEL_PATCH_KEY: ${AURA_SENTINEL_PATCH_KEY:-}
      DATABASE_ENGINE: cockroach
      COCKROACH_DATABASE_URL: ${COCKROACH_DATABASE_URL:-postgresql+asyncpg://root@aura-cockroach-us-east:26257/defaultdb?ssl=disable}
      DB_TRANSACTION_RETRIES: ${DB_TRANSACTION_RETRIES:-5}
      GLOBAL_REDIS_URLS: ${GLOBAL_REDIS_URLS:-local|redis://aura-redis:6379/0}
      REDIS_GLOBAL_SYNC_MODE: ${REDIS_GLOBAL_SYNC_MODE:-crdt-active-active}
      REDIS_RATE_LIMIT_SCOPE: ${REDIS_RATE_LIMIT_SCOPE:-global}
      REDIS_CONNECT_ATTEMPTS: ${REDIS_CONNECT_ATTEMPTS:-12}
      REDIS_OPERATION_RETRIES: ${REDIS_OPERATION_RETRIES:-4}
      REDIS_ENDPOINT_COOLDOWN_MAX_SECONDS: ${REDIS_ENDPOINT_COOLDOWN_MAX_SECONDS:-30}
      REDIS_URL: redis://aura-redis:6379/0
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}
      OTEL_EXPORTER_OTLP_INSECURE: ${OTEL_EXPORTER_OTLP_INSECURE:-true}
    volumes:
      - ./backend/app:/workspace/backend/app:rw
      - ./backend/app/migrations:/workspace/backend/app/migrations:rw
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    depends_on:
      - aura-cockroach-init
      - aura-redis
    networks:
      - aura-broker
      - aura-data
    restart: unless-stopped

  aura-redis:
    image: redis:7.4-alpine
    container_name: aura-redis
    command: ["redis-server", "--appendonly", "yes"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 6
    volumes:
      - aura-redis-data:/data
    networks:
      - aura-broker
    restart: unless-stopped

  aura-cockroach-us-east:
    image: cockroachdb/cockroach:v25.4.0
    container_name: aura-cockroach-us-east
    command: ["start", "--insecure", "--store=/cockroach/cockroach-data", "--listen-addr=0.0.0.0:26257", "--advertise-addr=aura-cockroach-us-east:26257", "--http-addr=0.0.0.0:8080", "--join=aura-cockroach-us-east:26257,aura-cockroach-eu-west:26257,aura-cockroach-ap-south:26257", "--locality=region=us-east,zone=us-east-a"]
    healthcheck:
      test: ["CMD", "cockroach", "sql", "--insecure", "--host=localhost:26257", "-e", "SELECT 1"]
      interval: 10s
      timeout: 5s
      retries: 12
    volumes:
      - aura-cockroach-us-east-data:/cockroach/cockroach-data
    networks:
      - aura-data
    restart: unless-stopped

  aura-cockroach-eu-west:
    image: cockroachdb/cockroach:v25.4.0
    container_name: aura-cockroach-eu-west
    command: ["start", "--insecure", "--store=/cockroach/cockroach-data", "--listen-addr=0.0.0.0:26257", "--advertise-addr=aura-cockroach-eu-west:26257", "--http-addr=0.0.0.0:8080", "--join=aura-cockroach-us-east:26257,aura-cockroach-eu-west:26257,aura-cockroach-ap-south:26257", "--locality=region=eu-west,zone=eu-west-a"]
    healthcheck:
      test: ["CMD", "cockroach", "sql", "--insecure", "--host=localhost:26257", "-e", "SELECT 1"]
      interval: 10s
      timeout: 5s
      retries: 12
    volumes:
      - aura-cockroach-eu-west-data:/cockroach/cockroach-data
    networks:
      - aura-data
    restart: unless-stopped

  aura-cockroach-ap-south:
    image: cockroachdb/cockroach:v25.4.0
    container_name: aura-cockroach-ap-south
    command: ["start", "--insecure", "--store=/cockroach/cockroach-data", "--listen-addr=0.0.0.0:26257", "--advertise-addr=aura-cockroach-ap-south:26257", "--http-addr=0.0.0.0:8080", "--join=aura-cockroach-us-east:26257,aura-cockroach-eu-west:26257,aura-cockroach-ap-south:26257", "--locality=region=ap-south,zone=ap-south-a"]
    healthcheck:
      test: ["CMD", "cockroach", "sql", "--insecure", "--host=localhost:26257", "-e", "SELECT 1"]
      interval: 10s
      timeout: 5s
      retries: 12
    volumes:
      - aura-cockroach-ap-south-data:/cockroach/cockroach-data
    networks:
      - aura-data
    restart: unless-stopped

  aura-cockroach-init:
    image: cockroachdb/cockroach:v25.4.0
    container_name: aura-cockroach-init
    entrypoint: ["/bin/sh", "-c"]
    command: ["cockroach init --insecure --host=aura-cockroach-us-east:26257 || true"]
    depends_on:
      - aura-cockroach-us-east
      - aura-cockroach-eu-west
      - aura-cockroach-ap-south
    networks:
      - aura-data
    restart: "no"

networks:
  aura-edge:
    driver: bridge
  aura-broker:
    driver: bridge
    internal: true
  aura-data:
    driver: bridge
    internal: true
  aura-p2p:
    driver: bridge

volumes:
  aura-redis-data:
  aura-cockroach-us-east-data:
  aura-cockroach-eu-west-data:
  aura-cockroach-ap-south-data:
